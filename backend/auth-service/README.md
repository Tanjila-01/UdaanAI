# Authentication Service

The **Authentication Service** manages user identities, login, registration, and RBAC in future phases.

## Phase 1 Responsibilities
- Service initialization and configuration
- Health check endpoint (`GET /health`)
- DB schema ownership: `auth`

## Port Allocation
- Default Port: `8001`
