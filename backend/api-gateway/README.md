# API Gateway Service

The **API Gateway Service** serves as the single backend entry point for client applications.

## Phase 1 Responsibilities
- Gateway health endpoint (`GET /health`)
- CORS handling for local frontend development (`http://localhost:5173`)
- Fast API Swagger documentation (`/docs`)

## Port Allocation
- Default Port: `8000`
